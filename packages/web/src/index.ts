// @outlays/web — public web client.
//
// Becomes a Next.js app in S7 (two-sided ledger: balance ribbon, dimension switcher,
// provenance drawer, coverage badge). No client-side LLM calls, ever. For S0 this is a
// buildable placeholder so `pnpm -r build` is green on a clean checkout.

export const PLACEHOLDER = "outlays web (scaffolded in S0, built in S7)";
