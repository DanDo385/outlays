// Package ingest runs adapters per the CLI protocol, validates envelopes and facts, and
// hands rows to the store. Implemented in S5. S0: package marker only.
package ingest

// Version identifies the ingest package surface.
const Version = "0.0.0"
